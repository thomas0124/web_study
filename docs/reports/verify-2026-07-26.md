# 検証レポート

**Date**: 2026-07-26
**Plan**: docs/plans/active/2026-07-26-web-study-platform.md
**Branch**: feat/web-study-platform
**Verifier**: verifier subagent (`/verify`)

## サマリー

**総合判定**: **partial-pass** — 静的解析（`tsc --noEmit` / `next lint`）はゼロエラー、コース／レッスン構造は完全、ルーティングとSSGの契約は成立。ただし **Shiki シンタックスハイライトが未実装**（受け入れ基準および設計決定に対するドリフト）、および **README.md 未作成**（プラン フェーズ5-17 のドキュメントドリフト）。

## スペック適合性

| # | 受け入れ基準 | 判定 | エビデンス |
|---|-------------|------|-----------|
| AC-1 | `pnpm dev` でローカル開発サーバー起動 → トップページにコース一覧 | ✓ 静的検証内で満たされている | `app/page.tsx` が `getCourses()` を呼び難易度別グループ表示。`.next/server/app/index.html` が 34k で生成済み |
| AC-2 | 全10コース詳細ページ（`/courses/[slug]`） | ✓ 満たされている | `app/courses/[courseSlug]/page.tsx` + `generateStaticParams` が10コース分の静的パラメータを返却。ビルド成果物 `.next/server/app/courses/` に10ディレクトリ生成済み |
| AC-3 | 全レッスンページ（`/courses/[slug]/lessons/[slug]`） | ✓ 満たされている | `app/courses/[courseSlug]/lessons/[lessonSlug]/page.tsx` + `generateStaticParams` → `getAllLessonPaths()`。`.next/server/app` 配下に 81 個の pre-rendered HTML |
| AC-4 | 各レッスンに「前 / 次」ナビゲーション | ✓ 満たされている | `components/LessonNav.tsx` を lesson page がレンダリング。`lib/content.ts:getLesson` が `prev` / `next` を計算 |
| AC-5 | コードブロックがシンタックスハイライトされる | ✗ **未達** | `package.json` に `shiki` / `rehype-pretty-code` / `@shikijs/*` の依存なし。`next.config.ts` の `rehypePlugins: []` は空。`components/mdx/index.tsx` の `pre` / `code` はプレーンな Tailwind スタイリングのみで、実際のトークン色付けを行わない。プランの「Shiki」設計決定と整合しない |
| AC-6 | モバイル 375px でレイアウトが崩れない | ⚠ 部分的（静的にはOK） | Tailwind の `sm:` / `md:` / `lg:` 応答クラスが `app` / `components` で 2 箇所以上使用。実ブラウザでの目視は `/test` フェーズの担当 |
| AC-7 | `pnpm build` が警告・エラーなしで完了 | ✓ 満たされている | `.next/prerender-manifest.json` および全 pre-rendered HTML が存在。ユーザー提供のコンテキスト「83 static pages, no errors or warnings」と一致 |
| AC-8 | TypeScript 型エラーゼロ (`tsc --noEmit`) | ✓ 満たされている | `pnpm tsc --noEmit` が EXIT=0（出力なし＝エラーなし）。ログ: `docs/evidence/verify-2026-07-26-tsc.log` |
| AC-9 | 各レッスンが日本語800字以上 | ✓ 満たされている | 全69ファイルを `wc -m` した最小値: `content/courses/08-web-security/lessons/01-security-basics.mdx` の 1,634 文字。閾値 800 の2倍以上 |
| AC-10 | 存在しないURLは 404 | ✓ 満たされている | `app/not-found.tsx` 実装済み。動的ルートは `notFound()` を呼び出す（`app/courses/[courseSlug]/page.tsx:28`、`.../lessons/[lessonSlug]/page.tsx:31,34`）。`.next/server/app/_not-found.html` 生成済み |

## コンテンツ構造検証

**meta.json (10/10)** — 全ての `slug` / `title` / `description` / `level` / `order` / `lessonCount` フィールドが存在し、値も妥当:

| ディレクトリ | slug | level | order | lessonCount | 実MDX数 |
|-------------|------|-------|-------|-------------|---------|
| 01-web-basics | web-basics | 入門 | 1 | 6 | 6 ✓ |
| 02-html-css | html-css | 入門 | 2 | 8 | 8 ✓ |
| 03-javascript-basics | javascript-basics | 入門 | 3 | 8 | 8 ✓ |
| 04-javascript-advanced | javascript-advanced | 中級 | 4 | 7 | 7 ✓ |
| 05-react | react | 中級 | 5 | 8 | 8 ✓ |
| 06-nodejs-backend | nodejs-backend | 中級 | 6 | 7 | 7 ✓ |
| 07-database | database | 中級 | 7 | 6 | 6 ✓ |
| 08-web-security | web-security | 上級 | 8 | 7 | 7 ✓ |
| 09-infrastructure | infrastructure | 上級 | 9 | 6 | 6 ✓ |
| 10-professional | professional | 上級 | 10 | 6 | 6 ✓ |

合計 **69 MDX ファイル**（プラン合致）。`meta.lessonCount` と実ファイル数が全コースで一致。

## ルーティング検証

- `app/page.tsx` — トップページ ✓
- `app/courses/[courseSlug]/page.tsx` — コース詳細 + `generateStaticParams` @L11 ✓
- `app/courses/[courseSlug]/lessons/[lessonSlug]/page.tsx` — レッスン + `generateStaticParams` @L14 ✓
- `app/not-found.tsx` — 404 ✓
- `app/layout.tsx` — ルートレイアウト（Header/Footer 付き）✓

## シークレット走査

`(api[_-]?key|secret|password|token|bearer|BEGIN (RSA|OPENSSH|PRIVATE))` を全ソースに対して実行。ヒットはすべて **教材 MDX 内のサンプルコード**（`content/courses/06-nodejs-backend/`、`content/courses/08-web-security/`、`content/courses/09-infrastructure/`）で、認証・JWT・OWASP などを教える目的で意図的に含まれた文字列。

- `API_KEY=secret_key_here` は `.env` ファイル書き方のプレースホルダ例（`06-nodejs-backend/lessons/02-npm-modules.mdx:115`）
- `JWT_SECRET` / `AWS_SECRET_ACCESS_KEY` などは `process.env.*` 参照のみで、値の埋め込みはない

`.env*` ファイルはリポジトリに存在せず（`node_modules` / `.next` / `.git` 除外）、実アプリコードにハードコードされた秘密情報は **なし**。

## ドキュメントドリフト

- [x] `.claude/rules/` — 変更対象外（今回の実装スコープではない）
- [x] `CLAUDE.md` — 最新（プロジェクトメタなので変更不要）
- [ ] **README.md 不在** — プラン フェーズ 5-17「README 更新（セットアップ手順）」が未完了。ルート直下に `README.md` が存在しない
- [ ] **plan チェックリスト未更新** — `docs/plans/active/2026-07-26-web-study-platform.md` の「進捗チェックリスト」および「受け入れ基準」の `- [ ]` が全てチェックされていない（`.claude/rules/planning.md` の「タスク進行中は進捗チェックリストを更新し続けること」に抵触）

## 静的解析

### `pnpm tsc --noEmit`
```
EXIT=0   （出力なし＝型エラーゼロ）
```
ログ: `docs/evidence/verify-2026-07-26-tsc.log`

### `pnpm lint` (next lint)
```
✔ No ESLint warnings or errors
EXIT=0
```
`next lint is deprecated and will be removed in Next.js 16` の非推奨警告のみ（プランのスコープ外）。
ログ: `docs/evidence/verify-2026-07-26-lint.log`

### `./scripts/run-static-verify.sh`
```
ベリファイアが実行されませんでした。ドキュメントのみの変更のようです。
EXIT=0
```
ハーネスは `git diff` ベースで変更検出するが、当該実装は **すべて untracked (`??`)** のためスキップされた。フォールバックとして `verify-typescript.sh` は個別に呼ばず、代わりにプロジェクトの `pnpm tsc` / `pnpm lint` スクリプトを直接実行して検証済み。
ログ: `docs/evidence/verify-2026-07-26-static.log`

## 残っているギャップ（未検証項目）

- **Shiki の動作確認** — 依存すら未追加のため実行時挙動を確認する意味はない（実装ギャップとして AC-5 で fail 済み）。
- **モバイル 375px の実測** — 静的解析ではブレークポイント使用の有無しか判別できない。`/test` フェーズで Playwright / 手動確認が必要。
- **404 の動的挙動** — 存在しないスラッグでの `notFound()` 呼び出しはコード上は確認したが、実サーバー応答は未検証。`/test` の担当。
- **`getCourses()` のソート順** — `lib/content.ts:11-13` はディレクトリ名でソート、`app/page.tsx:5` は難易度でグループ化。同一難易度内での順序は `meta.order` を明示的にソートしていないが、ディレクトリ名 `01-` / `02-` のプレフィックスと `order` が一致するため偶然一致。将来 `meta.order` を変更するとバグる可能性 → 回帰テスト対象候補。

## 推奨する最小の追加ベリファイア

1. **`meta.json` スキーマ検証**（例: zod）— 手動で `node -e "..."` を打つ代わりに `pnpm content:check` として CI に組み込む。今回検出できたのはたまたま。
2. **`getAllLessonPaths()` の重複／null 検証** — 静的テスト 1 本で SSG の暗黙契約を守れる。
3. **Shiki 統合の smoke test** — `rehype-pretty-code` を導入したら「MDX 内 `` ```ts `` ブロックが `<span style="color:...">` を含む」ことを assert する snapshot テスト。

## 結論

**総合判定**: **partial-pass**

**fail 項目（`/pr` に進む前に修正必須）**:
- AC-5: Shiki シンタックスハイライト未実装 — `next.config.ts` に `rehype-pretty-code`（または Shiki 直接統合）を追加し、`shiki` 依存を `package.json` に加えること
- ドキュメントドリフト: `README.md` を作成し、セットアップ手順（`pnpm install` / `pnpm dev` / `pnpm build`）と MDX コンテンツ追加方法を記述
- ドキュメントドリフト: プラン `2026-07-26-web-study-platform.md` の受け入れ基準・進捗チェックリストを実装状況に合わせて更新

**pass 項目**: AC-1〜4, AC-6（部分）, AC-7〜10、およびコンテンツ構造・ルーティング・シークレット走査。

次のステップ: 上記 fail 項目を修正 → `/self-review` → `/verify` を再実行 → `/test` へ。fail が残る場合は `/test` に進む前に修正すること（`.claude/rules/post-implementation-pipeline.md` の「fail 判定 → 継続前に修正して停止」に従う）。
